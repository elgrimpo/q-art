[pytest]
# Isolate this prototype's pytest rootdir so it does not inherit the app's
# repo-root pytest.ini (which sets asyncio_mode for pytest-asyncio — a plugin
# this self-contained venv intentionally does not install). Keeps test output
# pristine.
