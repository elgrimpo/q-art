[pytest]
asyncio_mode = auto
testpaths = api/tests
markers =
    e2e: marks tests as end-to-end integration tests (real external services)
    novita: marks tests that make real Novita API calls (expensive, local-only)
